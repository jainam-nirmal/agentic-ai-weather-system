{
  "name": "weather-agentic-ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean:allure": "node scripts/clean-allure.js",
    "allure:generate": "allure generate reports/allure-results -o reports/allure-report --clean",
    "allure:open": "allure open reports/allure-report",
    "test": "npm run clean:allure && npx playwright test && npm run allure:generate",
    "weatherTests": "npm run clean:allure && npx playwright test tests/api/weather.spec.js && npm run allure:generate",
    "uploadFileTests": "npm run clean:allure && npx playwright test tests/uploadFile/allFileUpload.spec.js && npm run allure:generate",
    "demoTests": "npm run clean:allure && npx playwright test tests/demo-test && npm run allure:generate && npm run allure:open"
   
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.18.0",
    "chalk": "^5.6.2",
    "dotenv": "^17.4.2",
    "ollama": "^0.6.3",
    "playwright": "^1.61.0",
    "readline-sync": "^1.4.10",
    "browser": "^0.2.6",
    "jsonwebtoken": "^9.0.3"
    
  },
  "devDependencies": {
    "allure-commandline": "^2.42.1",
    "allure-js-commons": "^3.10.0",
    "allure-playwright": "^3.10.0",
    "@playwright/test": "^1.61.0",
    "@types/node": "^22.13.10"
  }
}
